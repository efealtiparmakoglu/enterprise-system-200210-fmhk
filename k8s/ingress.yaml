apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-system-200210-fmhk-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-system-200210-fmhk.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-system-200210-fmhk-service
            port:
              number: 80
